import { Link } from 'react-router-dom';

const Error = props => {
    return (
        <div>
            Error
            <Link to="/">Go to Home</Link>
        </div>
    );
};

export default Error;
